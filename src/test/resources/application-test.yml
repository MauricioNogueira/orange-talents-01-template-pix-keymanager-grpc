datasources:
  default:
    url: ${JDBC_URL:`jdbc:postgresql://localhost:5432/projeto_pix_test`}
    username: ${JDBC_USER:zup}
    password: ${JDBC_PASSWORD:password}
    driverClassName: ${JDBC_DRIVER:org.postgresql.Driver}